server:
  port: 10001
  servlet:
    context-path: /

# 数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/danmaku_video?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: "@Cqy15850479975"
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      jedis:
        pool:
          max-active: 20
          max-idle: 10
  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 1000MB
      max-request-size: 1000MB

# MybatisPlus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted_at
      logic-delete-value: now()
      logic-not-delete-value: null

# JWT配置
jwt:
  secret: aa32f93c2e714d2891a7d1234abcdef56789abcdef0123456789
  expire: 86400000 # 过期时间：24小时
  header: Access-Token

# 日志配置
logging:
  level:
    com.danmaku: info
    org.springframework: warn
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n"